# HTTP client for local Ollama / vLLM inference (no cloud dependency)
httpx>=0.27.0

# Graph database (embedded, air-gapped safe)
kuzu>=0.7.0

# Vector store & embeddings
chromadb>=0.5.0
sentence-transformers>=3.0.0

# Data validation
pydantic>=2.7.0

# Utilities
python-dotenv>=1.0.0
rich>=13.7.0
thefuzz>=0.22.0

# Testing
pytest>=8.0.0
pytest-asyncio>=0.23.0
